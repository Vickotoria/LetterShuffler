/* A program which reads a string and outputs all the possible way
   to arrange the letters.

   author Vickotoria
   since 2020-02-20
*/

#include <stdio.h>

#include "list.h"


int main(void) {

   //read the string and save it

   //find all possible combinations
   //save them in a list

   //find duplicates in the list
   //remove the duplicates

   //print out the elements in the list

   return 0;
}
